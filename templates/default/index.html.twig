{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>
    
     {% for person in people %}
        {% if person.ville == 'Paris' %}
            <ul>
                <li> {{person.nom}} </li>
                <li> {{person.prenom}} </li>
                <li> {{person.ville}} </li>
            </ul>
        {% else %}
            <p>L'utilisateur {{person.prenom}} {{person.nom}} n'est pas présent dans la ville demandée</p>
        {% endif %}
    {% else %}
        <p>Aucun utilisateur trouvé</p>
    {% endfor %}

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>C:/Users/willi/comdorcet/src/Controller/DefaultController.php</code></li>
        <li>Your template at <code>C:/Users/willi/comdorcet/templates/default/index.html.twig</code></li>
    </ul>
</div>
{% endblock %}
